scapy>=2.5.0
scikit-learn>=1.3.0
networkx>=3.0
numpy>=1.24.0
pandas>=2.0.0
pymongo>=4.6.0
dnspython>=2.4.0  # required for mongodb+srv:// URIs
fastapi>=0.104.0
uvicorn>=0.24.0
python-dotenv>=1.0.0
requests>=2.31.0
joblib>=1.3.0
dnspython>=2.4.0
python-multipart>=0.0.6
jinja2>=3.1.0
aiofiles>=23.2.0
pydantic>=2.0.0
httpx>=0.25.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
# --- yara-python Windows note ---
# yara-python needs Microsoft C++ Build Tools to compile on Windows.
# Pick ONE of these options:
#
# [OPTION 1 - Easiest] Pre-built unofficial wheel (no compiler needed):
#   pip install yara-python --find-links https://github.com/VirusTotal/yara-python/releases/latest
#
# [OPTION 2] Install Build Tools first, then uncomment the line below:
# yara-python>=4.3.0
#
# NOTE: The virus_engine already handles missing yara gracefully (try/except),
# so the app will run fine without it — YARA scanning simply stays disabled.
# --------------------------------
psutil>=5.9.0
watchdog>=3.0.0
websockets>=12.0
# v3.0 additions
reportlab>=4.0.0
fpdf2>=2.7.0
xgboost>=2.0.0
